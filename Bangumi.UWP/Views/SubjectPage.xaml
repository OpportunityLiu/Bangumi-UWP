<Page x:Class="Bangumi.UWP.Views.SubjectPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Bangumi.UWP.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      NavigationCacheMode="Enabled"
      mc:Ignorable="d">
  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <TextBlock Text="{x:Bind VM.Value.NameCN, Mode=OneWay, TargetNullValue='中文名'}" />
    <Image>
      <Image.Source>
        <BitmapImage UriSource="{x:Bind VM.Value.Images.Large, Mode=OneWay}" />
      </Image.Source>
    </Image>
  </Grid>
</Page>
